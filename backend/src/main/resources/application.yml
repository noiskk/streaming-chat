server:
  port: 8080

gemini:
  api:
    key: AIzaSyDNR0t_X_Uadh-oXJvrW5SMROohqMXgsOU
    model: ${GEMINI_MODEL:gemini-2.5-flash}

logging:
  level:
    io.netty.resolver.dns.DnsServerAddressStreamProviders: OFF

---
spring:
  config:
    activate:
      on-profile: dummy

gemini:
  api:
    key: not-needed-in-dummy-mode
